#include "UI.h"

#include <thread>

void UI::ShowHUD(Renderer* renderer, Level* level)
{
	//renderer->ResetTextureFromLastPosition(renderer->bufferWidth / 2, 25, renderer->bufferWidth, 50, level);

	//std::thread([]() { UI::ShowTextUI(); }).detach();
}